<form method="post" action="{{ path('app_klient_delete', {'id': klient.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ klient.id) }}">
    <button class="btn">Delete</button>
</form>
